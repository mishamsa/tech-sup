{
  "name": "tournament-landing",
  "version": "1.0.0",
  "description": "Landing page — Board Game Tournament Management System (Bachelor thesis)",
  "type": "module",
  "scripts": {
    "lint": "eslint src/**/*.js",
    "lint:fix": "eslint src/**/*.js --fix",
    "lint:css": "stylelint **/*.css",
    "lint:css:fix": "stylelint **/*.css --fix",
    "lint:all": "npm run lint && npm run lint:css",
    "lint:report": "eslint src/**/*.js --format html --output-file reports/eslint-report.html"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-html": "^8.0.0",
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^36.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "board-games",
    "tournament",
    "landing-page",
    "bachelor-thesis"
  ],
  "author": "",
  "license": "MIT"
}
